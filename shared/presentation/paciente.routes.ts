// Archivo de rutas compartido para pacientes
// Utilizado en CU-04 (Registrar Paciente Crónico) y CU-05 (Gestionar Perfil del Paciente)
// Define los endpoints relacionados con pacientes (ej. POST /pacientes, GET /pacientes/:id, PATCH /pacientes/:id).
// Mapea las solicitudes HTTP hacia el controlador correspondiente.

// ...aquí se definirán las rutas usando el framework correspondiente
